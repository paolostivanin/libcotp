language: c

branches:
  only:
    - master

compiler:
  - gcc
  - clang

build:
  ci:
    - sudo apt update
    - sudo apt install cmake libgcrypt20-dev
    - mkdir build && cd $_
    - cmake ../
    - make -j2
